{
  "name": "Entorno Airflow y GX Delivery 2",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "remoteEnv": {
    "AIRFLOW_HOME": "${containerWorkspaceFolder}",
    "AIRFLOW__CORE__LOAD_EXAMPLES": "False"
  },
  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "Airflow UI",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "pip install -r requirements.txt && airflow db init && airflow users create --username admin --firstname Bryan --lastname Dev --role Admin --email admin@example.com --password admin && python -m airflow standalone", 
  "containerEnv": {
  "AIRFLOW__WEBSERVER__ENABLE_PROXY_FIX": "True"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}